# ORBIT 최종 발표 플랜 v3 — 시연 4:30 + 기술 챌린지 재구성

작성일: 2026-07-19
기준 커밋: `019e97e6` (develop, 2026-07-17 이후 449커밋 반영)
추가 반영: PR #578 리허설 슬라이드 이미지 로딩 최적화
v2와의 차이: ① 시연을 7분 안의 5분에서 **독립 4:30(최대 4:50)** 로 재설계, ② 7/17 이후 신규 기능(슬라이드 자동 질문 생성, 발음 사전, Web Speech 온디바이스 경로, 품질 검증 안전 수정, 리허설 추이 대시보드, 슬라이드 레일 재정렬) 반영, ③ **기술 챌린지를 3장으로 확장하고 시도→문제→해결 서사로 재구성**.

---

## 0. v2 이후 바뀐 사실 — 대본에 반드시 반영해야 하는 것

| # | 변화 | v2 대본의 문제 | v3 처리 |
| --- | --- | --- | --- |
| 1 | 라이브 STT가 **포트 4구현 체제**(sherpa / moonshine / Web Speech / OpenAI Realtime)로 정리됨. `apps/web/src/features/rehearsal/stt/README.md` | "음성이 서버로 가지 않는 온디바이스 STT"를 단정 | **표현 교체** (아래 §1 주의) |
| 2 | Web Speech 포트가 `processLocally: true`, `quality: "command"`, `maxAlternatives: 3` 고정. `webSpeechLiveSttPort.ts` | 없음 | 관전 포인트 ②의 "어떻게"로 승격 |
| 3 | 발음 사전(alias) 계약 + 실시간/종료 후 적용. `packages/shared/src/pronunciation/*` (PR #539) | 없음 | 챌린지 ②의 새 4단계 |
| 4 | 인식 후보 재랭킹 `alternativeReranker.ts` (`scoreBiasMatch ≥ 0.75`일 때만 교체) | 없음 | 챌린지 ②의 핵심 근거 |
| 5 | 슬라이드 **예상 질문 자동 생성** — 에디터 진입 시 전 슬라이드 배치 예약, frozen deck snapshot, `slide-text-v1` 해시, concurrency 2 | 기능 자체가 없었음 | **시연 STEP 3으로 신규 편성** |
| 6 | 품질 검증 패널 focus + 안전 수정 루프 구현 (PR #527/#530) | "하지 않는 조작"으로 제외했었음 | 시연에 편입 (짧게) |
| 7 | 슬라이드 레일 추가 메뉴·**순서 변경** 구현 | "미지원 🟡"로 회피 | 회피 목록에서 제거 |
| 8 | 리허설 프로젝트 추이 대시보드 (`RehearsalProjectSummaryDashboard.tsx`, PR #538) | 없음 | 리포트 씬을 "1회차 리포트"→"회차 추이"로 강화 |
| 9 | Deck materialization 트랜잭션 경로 / 발표 세션·Activity의 최신 Deck 버전 연결 (PR #550) | 없음 | 챌린지 ①의 마지막 막(선택) |
| 10 | PPTX/PNG 내보내기 + OOXML 동기화 통합 검증 | PDF만 배제 | 내보내기는 시연 시간상 생략(§3 회피표) |
| 11 | 다음 슬라이드 미리 준비·이미지 재사용·중복 전송 제거 (PR #578) | 로컬에서는 드러나지 않았던 배포 환경 이미지 지연 | **챌린지 ③으로 신규 편성**, STEP 4 자동 전환 장면과 연결 |

### §1 온디바이스 표현 — 이렇게만 말한다

`stt/README.md`는 명시적으로 이렇게 적어 두었다: *"Web Speech is treated as non-local for consent purposes. MDN documents that some browsers, including Chrome, can use a server-based recognition engine."*

- ❌ 쓰지 말 것: "제 음성은 서버로 가지 않습니다", "완전 온디바이스입니다"
- ✅ 쓸 것: **"브라우저의 온디바이스 인식 경로(`processLocally`)를 요청하고, 인식 결과를 고르는 판단은 전부 브라우저 안에서 끝납니다"**
- 근거를 물으면: 포트 계약(`LiveSttPort`) 덕분에 온디바이스 엔진(sherpa/moonshine)과 클라우드 엔진(OpenAI Realtime)을 환경별로 갈아 끼울 수 있고, 시연은 Chrome Web Speech 온디바이스 경로로 돌린다.

---

## 1. 시연 4:30 — 씬 구성

원칙 3가지.
1. **한 프로젝트, 한 궤도.** 탭 전환·로그인·목록 왕복 없음. 화면 이동은 6번.
2. **기다림은 전부 사전 처리.** 생성·분석·질문 생성은 "이미 돌아가 있는" 상태로 만난다. 로딩 화면을 보여주는 유일한 지점은 STEP 1의 15초뿐.
3. **말이 화면을 움직이는 장면을 두 번 넣는다.** (STEP 4 키워드 자동 전환, STEP 6 청중 응답) — 관객이 기억하는 건 이 둘이다.

| STEP | 구간 | 길이 | 화면 | 보여줄 것 |
| --- | --- | --- | --- | --- |
| 1 | 0:00–0:55 | 55s | 브리프 → 생성 → 스토리 플랜 → 스타일 | "누구에게·왜"부터 묻는 입구, 승인 게이트, 백그라운드 생성 |
| 2 | 0:55–1:35 | 40s | 편집기 | 초안 훑기 → 노트·키워드 → 품질 경고 1건 **안전 수정** |
| 3 | 1:35–2:05 | 30s | 편집기 QnA 탭 | **슬라이드별 예상 질문이 이미 만들어져 있음** |
| 4 | 2:05–3:05 | 60s | 리허설 | 프리플라이트 → 발화 → **키워드 인식으로 자동 전환** (+발음 사전) |
| 5 | 3:05–3:35 | 30s | 리포트 | 지난 회차 리포트 → **회차 추이 대시보드** |
| 6 | 3:35–4:30 | 55s | 발표 + 청중 | 발표자 화면 → QR → 객석 응답 → 실시간 결과 |

**4:50 버전**: STEP 1에 +10s(핵심 메시지 입력을 실제 타이핑), STEP 4에 +10s(발음 alias가 잡히는 순간을 한 번 더 짚음). 그 외는 손대지 않는다.

### 화면 전환 6회 지점 (미리 단축키/탭 배치)
0:55 편집기 진입 · 1:35 QnA 탭 · 2:05 리허설 · 3:05 리포트 · 3:35 발표 모드 · 4:05 청중 화면(보조 기기 미러링)

---

## 2. 시연 대본 (초 단위)

괄호는 행동 지시. 〈 〉는 발표 전 확정할 값.

### STEP 1 — 브리프 → 생성 (0:00, 55s)

*(새 프로젝트 화면이 이미 열려 있는 상태에서 시작)*

> **0:00** 3일 뒤 최종 발표를 앞둔 정글러가 되어 보겠습니다. ORBIT은 슬라이드부터 묻지 않습니다. **누구에게, 왜 말하는지**부터 묻습니다.
>
> *(브리프: 청중=심사위원, 시간=7분, 핵심 메시지 3개 — 사전 입력 상태에서 마지막 한 줄만 타이핑)*
>
> **0:20** 생성을 누르면 — 슬라이드가 바로 쏟아지지 않습니다. **발표의 뼈대인 스토리 플랜을 먼저 제안하고, 제 승인을 기다립니다.**
>
> *(스토리 플랜 화면. 한 섹션 제목을 손으로 고친 뒤 승인)*
>
> **0:35** 여기서 흐름을 고치면 뒤의 모든 슬라이드가 그 흐름으로 만들어집니다. 승인. 지금부터 뒤에서는 기획 4단계와 슬라이드별 실행 단계가 병렬로 돌아갑니다. 그 사이 저는 톤만 고르면 됩니다.
>
> *(스타일·색 선택 → 생성 진행 화면에서 완성된 슬라이드가 순서대로 채워지는 것을 3초간 보여준 뒤 사전 생성 프로젝트로 전환)*

⏱ 체크: 0:55에 편집기가 떠 있어야 한다.

### STEP 2 — 초안·편집 (0:55, 40s)

> **0:55** 초안이 나왔습니다. 브리프의 핵심 메시지가 슬라이드와 **발표자 노트**에 그대로 살아 있고, 노트에서 뽑힌 이 **키워드**들이 곧 리허설의 재료가 됩니다.
>
> *(우측 패널 키워드 인스펙터 3초)*
>
> **1:15** 초안이니 손은 봐야죠. 이 슬라이드, 텍스트가 넘칩니다. AI 품질 검사가 이미 잡아 뒀고 — *(경고 클릭 → 해당 객체로 포커스 이동)* — 경고를 누르면 문제 객체로 바로 갑니다. 수정을 누르면 **레이아웃을 깨지 않는 범위에서만** 고칩니다.

⏱ 체크: 안전 수정 애니메이션이 끝나는 순간 바로 QnA 탭으로.

### STEP 3 — 예상 질문 자동 생성 (1:35, 30s) 🆕

> **1:35** 그런데 발표에서 진짜 무서운 건 슬라이드가 아니라 **질문**이죠.
>
> *(QnA 탭 열기 — 이미 채워져 있음)*
>
> **1:45** 제가 아무것도 누르지 않았는데 슬라이드마다 예상 질문이 준비돼 있습니다. 편집기에 들어온 순간 전 슬라이드 분량이 한 번에 예약됐고, **질문을 만들 때 본 슬라이드는 그 시점에 얼어붙은 스냅샷**입니다. 그래서 옆 슬라이드 색을 바꿔도 이 질문은 사라지지 않고, **제목·본문·대본이 실제로 바뀔 때만** 다시 만듭니다.

💡 이 30초가 v3에서 새로 얻은 가장 강한 장면이다. "내가 누르지 않았는데 이미 있다"를 반드시 소리 내어 말할 것.

### STEP 4 — 리허설 (2:05, 60s) ★ 하이라이트

> **2:05** 이제 이 덱으로 연습합니다. 마이크 확인 — *(프리플라이트 통과)*
>
> **2:20** *(실제 발화, 대본 그대로 읽지 않기)* "…그래서 저희는, 음, 발표 준비의 **전 과정을 한 궤도로** 묶었습니다."
>
> *(슬라이드 자동 전환 — 다음 장의 이미지가 준비된 상태로 한 번에 나타나는지 확인)*
>
> **2:35** 보셨나요. **제가 넘기지 않았고, 다음 장도 바로 나타났습니다.** 그리고 지금 제가 읽은 문장, 대본과 글자가 다릅니다. 대본엔 "전체 궤도를 하나로"라고 적혀 있었는데 저는 "전 과정을 한 궤도로"라고 말했죠. 그래도 넘어갑니다.
>
> **2:50** 브라우저가 인식 후보를 여러 개 받아서 이 발표의 **고유 용어와 발음 사전으로 다시 고르고**, 그 다음 문장 **의미**가 대본과 같은지를 판단합니다. 이게 다 브라우저 안에서 끝납니다.

⚠ 실패 대비: 1회 재발화 → 그래도 안 되면 "지금은 수동으로 넘기고, 인식 로그는 리포트에서 보시겠습니다"로 **STEP 5에 자연스럽게 흡수**. 절대 두 번 이상 재시도하지 않는다.

### STEP 5 — 리포트·추이 (3:05, 30s)

> **3:05** 연습을 마치면 이런 리포트가 남습니다. 어디서 멈췄는지, 얼마나 빨랐는지, **핵심 메시지를 실제로 입 밖에 냈는지**를 커버리지로 보여줍니다.
>
> **3:20** 그리고 이건 1회차가 아니라 **누적**입니다. *(추이 대시보드)* 회차가 쌓이면 속도가 잡히는지, 침묵이 줄어드는지가 그래프로 보입니다. 연습이 기록으로 남는 게 ORBIT의 목적입니다.

### STEP 6 — 라이브 + 청중 (3:35, 55s)

> **3:35** 연습했으니 무대로 갑니다. *(발표 모드)* 바깥 화면엔 슬라이드, 제 앞엔 노트·타이머·다음 키워드가 있는 발표자 화면.
>
> **3:50** 그리고 이 QR — *(객석 팀원 접속)* 지금 객석에서 들어오고 있습니다. 답변해 주시면
>
> **4:10** *(실시간 결과가 차오름)* 결과가 무대 화면에 실시간으로 쌓입니다.
>
> **4:20** 브리프 한 장에서 시작한 발표가, 객석의 반응으로 끝났습니다. **이게 ORBIT의 한 궤도입니다.**

---

## 3. 이번에도 하지 않는 것

| 하지 않는 조작 | 이유 |
| --- | --- |
| viewer 계정 열기 / 공유 링크 | 편집기 역할 게이팅 미완 🟡, 공개 링크 🔴 |
| PDF 내보내기 | 🔴 미지원. PPTX·PNG는 되지만 **시간상 생략** |
| 캔버스 zoom·nudge | 🔴 미구현 |
| 실시간 공동 편집 | 🟠 서버 게이트웨이만 존재 |
| 리허설 직후 분석 리포트 대기 | 워커 지연 리스크 — 사전 회차 사용 |
| 청중 화면 스트리밍 공유 | 🟡 MVP — QR 입장·설문만 |
| 도전 Q&A / 집중 연습 세션 | 구현돼 있지만 4:30에 안 들어감. **포스터 세션용으로 남긴다** |

### 리스크·백업

| 리스크 | 대응 (10초 내) |
| --- | --- |
| STEP 1 생성 지연 | "미리 만들어 둔 프로젝트로 보겠습니다" → 사전 생성 탭 |
| STEP 3 질문이 비어 있음 | `SLIDE_QUESTION_GUIDES_ENABLED` 사전 확인 필수. 비면 생성 완료된 프로젝트로 전환 |
| STEP 4 키워드 미인식 | 재발화 1회 → 수동 전환 후 STEP 5로 흡수 |
| STEP 4 이미지 준비 지연 | 현재 슬라이드를 유지하고 최대 3초 후 placeholder로 진행. 시연 전 같은 프로젝트를 1회 왕복해 warm 경로 확인 |
| STEP 4 마이크 권한 팝업 | 시연 직전 같은 브라우저 프로필로 1회 허용해 둘 것 |
| STEP 6 와이파이 | 핫스팟 사전 테스트, 실패 시 발표자 화면의 결과 미리보기로 대체 |
| 전체 실패 | 백업 영상 4:30 — 로컬 파일 재생 대기 |

---

## 4. 기술적 챌린지 재구성 (주제 유지 + 배포 후 발견한 문제 추가)

세 장 모두 **결과 나열이 아니라 `시도 → 어떤 문제가 생겼는지 → 다음 시도`** 순서로 쓴다. 각 장의 마지막 줄은 **데모에서 본 장면**으로 연결한다. 챌린지 ③은 로컬에서는 보이지 않았던 문제가 배포 후 드러났다는 흐름으로 시작해, 실제 운영 환경에서 병목을 추적하고 해결한 경험을 보여준다.

### S6 — 챌린지 ① 덱 생성: 재시도 범위 줄이기

v2의 문제: "9개 stage로 나눴다"는 결과만 있고 그 선택의 이유가 없었다. v3은 **재시도 범위**라는 기준 하나로 세 시도를 비교한다.

장표 (16:9):

```
┌──────────────────────────────────────────────────────────────────────────────────────────────┐
│                                                                                              │
│   【 챌린지 ①  덱 생성 — 재시도 범위 】                                                      │
│                                                                                              │
│                                                                                              │
│    실패하면 다시 실행해야 하는 범위                              ┌─ planning ─────┐          │
│                                                                  │ × 4 stage      │          │
│    ① 단일 호출      ████████████████████████████   덱 전체       └────────────────┘          │
│                     수 분간 빈 화면                                                          │
│                                                                  승인 게이트 (사용자)        │
│    ② stage 분할     ████   조각                                                              │
│                     9 stage + 슬라이드 shard + checkpoint        ┌─ execution ────┐          │
│                                                                  │ × 슬라이드     │          │
│    ③ Postgres 큐    █   트랜잭션                                 └────────────────┘          │
│                     SKIP LOCKED 선점 + lease                                                 │
│                                                                                              │
│                                                                                              │
│                                                                                              │
│    ①→②  실패한 조각만 재시도        ②→③  큐와 상태를 한 곳에                                 │
│                                                                                              │
│                                                                                              │
│                                                                                              │
│    ▸ 완성된 슬라이드부터 순서대로 미리보기                                                   │
│                                                                                              │
│                                                                                              │
│                                                                                              │
└──────────────────────────────────────────────────────────────────────────────────────────────┘
```

제작 노트: 좌측 막대 3개가 **길이로 재시도 범위를 보여주는 것**이 이 장표의 전부다. 실제 슬라이드에서는 막대를 애니메이션으로 ①→②→③ 순서대로 줄여서 보여준다. 우측 파이프라인은 배경 톤으로 낮춰 배치. 본문 텍스트는 20pt 이상, 막대 라벨만 강조색.

대본:

> 처음엔 한 번의 호출로 덱 전체를 만들었습니다. 실패하면 다시 실행해야 하는 범위가 **덱 전체**였습니다. 수 분간 빈 화면이고, 마지막 슬라이드에서 실패하면 처음부터 다시였습니다.
>
> 그래서 생성을 아홉 개 stage와 슬라이드 단위 조각으로 나누고, 조각마다 checkpoint를 남겼습니다. 재시도 범위가 조각 하나로 줄었습니다. 다만 여기서 다른 문제가 나왔습니다. **큐는 Redis에, 상태는 DB에** 있어서 둘이 어긋나면 실행 중으로 표시됐지만 실제로는 아무도 처리하지 않는 잡이 생겼습니다.
>
> 그래서 큐 자체를 Postgres 테이블로 옮겼습니다. `SKIP LOCKED`로 워커가 잡을 선점하고, 선점과 산출물 저장과 상태 전이가 **한 트랜잭션** 안에서 끝납니다. 상태를 두 곳에 쓰지 않으니 어긋날 수 없습니다.
>
> 데모에서 완성된 슬라이드가 순서대로 채워졌던 것이 이 구조의 결과입니다.

근거 코드: `apps/worker/src/generate-deck/postgres-stage-runner.ts`, `stage-checkpoint-repository.ts`, `execution-stage.processor.ts`, 커밋 `f732449aa`·`6b1ee637e`.

**선택 확장(시간 여유 시 20초)** — "다음 문제는 큐가 아니라 산출물이었습니다. 생성된 덱을 발표 세션과 청중 활동이 서로 다른 버전으로 보고 있어서, Deck materialization을 트랜잭션 경로로 묶었습니다." (PR #550, `e82a50ff`·`7db646b6`)

### S7 — 챌린지 ② 대본과 다르게 말해도 인식하기

v2의 문제: "문자열 → 동의어 → E5"에서 끝나 **"인식 자체가 틀리면 어떻게 되나"** 에 답이 없었다. v3은 E5를 유지하되 앞의 **인식 단계**와 뒤의 **판정 단계**를 붙여 3단계로 설명한다.

장표 (16:9):

```
┌──────────────────────────────────────────────────────────────────────────────────────────────┐
│                                                                                              │
│   【 챌린지 ②  발표자는 대본대로 읽지 않는다 】                                              │
│                                                                                              │
│                                                                                              │
│    정확 문자열 매칭  ✗ 조사·어미 하나에 실패                                                 │
│    동의어·약어 사전  ✗ 사전에 없는 표현은 놓침                                               │
│                                                                                              │
│    ┌─ 1 인식 ───────────┐       ┌─ 2 의미 ───────────┐       ┌─ 3 판정 ───────────┐          │
│    │ 후보 3개           │    →  │ E5-small 임베딩    │    →  │ 문턱 3개           │          │
│    │ + 발음 사전        │       │ query / query      │       │ 통과해야만 전환    │          │
│    └────────────────────┘       └────────────────────┘       └────────────────────┘          │
│                                                                                              │
│                                                                                              │
│                                                                                              │
│    유사도  ├──────────────────────────────┼──────────────────┤                               │
│           0.0            ad-lib          0.89        covered / paraphrased  1.0              │
│                       슬라이드 유지                    슬라이드 전환                         │
│                                                                                              │
│                                                                                              │
│    ▸ 마진 0.04 · 어휘겹침 0.55      ▸ 검증  p95 ≤ 500ms · 오탐 ≤ 1%                          │
│                                                                                              │
│                                                                                              │
│                                                                                              │
│                                                                                              │
│                                                                                              │
└──────────────────────────────────────────────────────────────────────────────────────────────┘
```

제작 노트: 상단 3박스는 왼쪽부터 하나씩 등장. 하단 **유사도 축이 핵심 시각 요소**다 — 0.89 왼쪽은 회색(슬라이드 유지), 오른쪽은 강조색(전환)으로 칠하고, 데모에서 실제로 인식된 문장의 점수 하나를 축 위에 점으로 찍어두면 대본의 마지막 문장과 바로 연결된다. 0.75(재랭킹 게이트)는 축에 넣지 말고 말로만 언급 — 축이 두 개가 되면 읽히지 않는다.

대본:

> 두 번째. 발표자는 대본대로 읽지 않습니다. 문자열 매칭은 조사 하나만 달라져도 실패했고, 동의어 사전은 사전에 없는 표현을 놓쳤습니다. 그래서 의미로 비교하기로 했는데, 그 앞에 문제가 하나 더 있었습니다. **인식 결과가 틀리면 의미 비교는 시작할 수 없습니다.**
>
> 그래서 세 단계로 나눴습니다. 1단계는 인식입니다. 브라우저에서 인식 후보를 **세 개** 받고, 이 발표의 고유 용어와 발음 사전으로 후보를 다시 점수 매겨 기준을 넘을 때만 1순위를 교체합니다. 2단계는 의미 비교입니다. 인식된 문장을 E5 임베딩으로 대본 문장과 비교하고, prefix 모드는 실측으로 `query/query`를 선택했습니다.
>
> 3단계가 판정인데, 여기가 핵심이었습니다. **임베딩은 상관없는 말을 해도 항상 가장 가까운 문장을 하나 돌려줍니다.** 그래서 유사도 0.89, 1등과 2등의 차이 0.04, 어휘 겹침 0.55 — 세 개의 기준을 실측으로 정했습니다. 하나라도 못 넘으면 애드리브로 분류하고 **슬라이드를 넘기지 않습니다.** 잘못 넘어가는 쪽이 안 넘어가는 쪽보다 발표에 더 치명적이기 때문입니다.
>
> 브라우저 벤치마크로 p95 500ms, 오탐 1% 이하를 기준으로 검증했습니다. 데모에서 제가 대본과 다르게 말했는데도 넘어간 것이 이 세 단계의 결과입니다.

근거: `apps/web/src/features/rehearsal/speech/semanticUtteranceDecision.ts` (0.89 / 0.04 / 0.55), `e5EmbeddingService.ts` (`E5_PREFIX_MODE = "query-query"`), `stt/alternativeReranker.ts` (0.75 게이트), `stt/koreanTextSimilarity.ts`, `packages/shared/src/pronunciation/*`, `docs/spikes/e5-prefix-mode-calibration.md`, `docs/spikes/semantic-cue-browser-nli-poc.md`.

### S8 — 챌린지 ③ 리허설: 이미지가 준비된 뒤 슬라이드 넘기기

핵심: 로컬에서는 보이지 않던 느린 전환이 서버 배포 후 드러났다. 확인해 보니 **이미지가 준비되기 전에 화면부터 넘기고, 같은 이미지도 반복해서 준비하는 구조**가 원인이었다.

> ⚠ 아래 성능 수치는 발표 구성용 임시값이다. 최종 발표 전 같은 서버·브라우저 조건에서 다시 측정해 교체한다.

장표 (16:9):

```
┌──────────────────────────────────────────────────────────────────────────────────────────────┐
│                                                                                              │
│   【 챌린지 ③  배포 후 느려진 슬라이드 전환 】                                               │
│                                                                                              │
│    문제     로컬에서는 즉시 표시  →  서버에서는 이미지가 뒤늦게 나타남                      │
│                                                                                              │
│    이전                                                                                      │
│    화면부터 전환  ─→  그제야 이미지 요청·준비  ─→  뒤늦게 표시                               │
│                    같은 이미지도 반복 준비                                                  │
│                                                                                              │
│    개선                                                                                      │
│    다음 이미지 미리 준비  ─→  준비된 이미지 재사용  ─→  그다음 화면 전환                     │
│                    같은 파일은 다시 전송하지 않음                                            │
│                                                                                              │
│    ▸ 첫 전환      2.2초  →  0.6초                                                          │
│    ▸ 재방문       1.3초  →  0.05초       이미지 재전송  1.7MB  →  0                       │
│    ▸ 반복 테스트  1 → 2 → 1 → 2에서 각 이미지 요청 1회 · 숨겨진 다음 화면 0개              │
│                                                                                              │
└──────────────────────────────────────────────────────────────────────────────────────────────┘
```

제작 노트: 장표에서는 **"화면부터 전환"과 "이미지부터 준비"의 순서 차이**만 크게 보여준다. 전후 수치는 `2.2초 → 0.6초`, `1.3초 → 0.05초`만 크게 강조한다. 현재 값은 발표 구성용 임시값이므로 최종 장표에는 운영 실측값만 넣는다.

대본:

> 세 번째 챌린지는 슬라이드 이미지 로딩입니다. 로컬에서는 이미지가 바로 나타나서 문제가 없는 줄 알았습니다. 그런데 서버에 배포하니 다음이나 이전 슬라이드를 누를 때 이미지가 뒤늦게 나타났습니다.
>
> 처음에는 단순히 서버가 느린 문제라고 생각했습니다. 하지만 슬라이드를 반복해서 넘기며 확인해 보니, 화면을 먼저 바꾼 뒤에 이미지를 요청하고 준비하고 있었습니다. 같은 이미지를 다시 볼 때도 이 준비 과정이 반복됐고, 다음 장을 위해 보이지 않는 큰 화면까지 따로 그리고 있었습니다.
>
> 그래서 순서를 반대로 바꿨습니다. 다음 슬라이드의 이미지를 미리 준비하고, 한 번 준비한 이미지는 다시 사용했습니다. 이미지가 준비된 뒤에만 화면을 전환하고, 보이지 않는 다음 화면을 미리 그리던 작업은 제거했습니다. 서버도 같은 파일은 다시 보내지 않도록 바꿨습니다.
>
> 임시 측정에서는 첫 전환이 2.2초에서 0.6초로, 다시 방문할 때는 1.3초에서 0.05초로 줄었습니다. 이미지 재전송량도 1.7MB에서 0이 됐습니다. 이전에는 버튼을 누른 뒤 시작하던 일을 이제는 버튼을 누르기 전에 끝내 놓기 때문에 전환이 빨라졌습니다.

근거: `apps/web/src/features/slides/rendering/slideImageCache.ts`, `apps/web/src/features/rehearsal/presenter/slideAssetNavigationGate.ts`, `apps/web/src/features/rehearsal/RehearsalWorkspace.tsx`, `apps/api/src/files/files.service.ts`, `packages/storage/src/index.ts`, `tests/e2e/slideshow-transitions.spec.ts`, PR #578 (`2d273d0c`·`05ac9ff2`·`220d45f6`).

### 왜 이 세 장이 v2보다 나은가

| | v2 | v3 |
| --- | --- | --- |
| 기준 | 기능 나열 | 비교 기준 하나 (①재시도 범위 / ②처리 단계 / ③이미지 준비 순서) |
| 반박 대응 | "인식 오류는?", "서버가 느린 것 아닌가?"에 답 없음 | 인식 오류와 느린 이미지 전환의 실제 원인을 설명 |
| 수치 | 없음 | 의미 판정 문턱·이미지 요청 횟수는 코드와 테스트 근거, 이미지 응답 시간은 최종 실측 후 확정 |
| 연결 | 데모와 별개 | 세 장 모두 마지막 줄이 데모 장면으로 연결 |
| 트레이드오프 | 없음 | 정확도·속도·실패 대응 중 무엇을 우선했는지 명시 |

---

## 5. 예상 질문 대비 (포스터/QA 30초 답변)

| 질문 | 답 |
| --- | --- |
| "온디바이스라면서 정확도는요?" | 온디바이스 인식 경로를 요청하되 정확도는 후처리로 방어합니다 — 후보 3개 재랭킹 + 발음 사전. 엔진은 `LiveSttPort` 계약이라 sherpa/moonshine/OpenAI Realtime으로 교체 가능합니다 |
| "0.89 같은 숫자는 어떻게 정했나요?" | 한국어 발표 픽스처(정확 낭독/노이즈/패러프레이즈/인접 문장/애드리브/혼합 발화)로 prefix 모드 2종을 비교 측정했습니다. 우선순위는 **애드리브 오탐 최소화**였습니다 |
| "Redis 대신 Postgres 큐면 느리지 않나요?" | 덱 생성은 stage당 수 초 단위 LLM 호출이 지배적이라 큐 오버헤드가 병목이 아닙니다. 대신 큐-DB 이중 쓰기가 사라졌습니다. 음성 분석처럼 짧고 잦은 잡은 그대로 BullMQ입니다 |
| "예상 질문이 stale되지 않나요?" | 제목·본문·대체텍스트·대본만 canonical 해시에 넣습니다(`slide-text-v1`). 도형 색·위치를 바꿔도 질문은 유지됩니다 |
| "브라우저 캐시만 쓰면 되지 않나요?" | 브라우저 캐시는 파일 전송은 줄여도 이미지 준비가 끝났는지까지 공유하지 못합니다. 그래서 다음 이미지를 미리 준비하고, 준비된 결과 자체를 재사용했습니다 |
| "왜 이미지를 계속 저장해 두지 않나요?" | 프로젝트 접근 권한이 사라지면 이미지도 볼 수 없어야 합니다. 그래서 권한은 매번 확인하되, 같은 파일이면 내용은 다시 보내지 않도록 했습니다 |
| "5명이 어떻게 병렬로?" | 모든 경계가 Zod 공통 계약(`packages/shared`)입니다. 계약을 먼저 머지하고 웹·API·워커·파이썬이 각자 붙였습니다 |
| "협업 편집은요?" | 게이트웨이는 있고 편집기 미연결입니다. MVP 범위에서 **의도적으로 뺐습니다** — 개인 발표 준비가 타깃이라 |

---

## 6. 제작 체크리스트 (v3 delta)

- [ ] **STEP 3 사전 조건**: 시연 계정 환경에 `SLIDE_QUESTION_GUIDES_ENABLED=true` 확인 + 에디터 1회 진입해 전 슬라이드 질문 생성 완료시켜 둘 것
- [ ] **STEP 4 엔진 고정**: Web Speech, `processLocally` 다운로드 완료 상태(`SpeechRecognition.install`) 사전 확인 — 첫 실행 시 모델 다운로드가 뜨면 시연이 죽는다
- [ ] STEP 4 발음 사전에 〈ORBIT / 오르빗 / 정글 / E5〉 등 alias 사전 등록
- [ ] STEP 4 발화 문장을 **대본과 일부러 다르게** 확정하고 3회 이상 인식 성공 확인
- [ ] STEP 2용 텍스트 넘침 슬라이드 1장 사전 배치
- [ ] STEP 5용 회차 3개 이상 누적(추이 그래프가 선으로 보여야 함)
- [ ] S7 도표 — 0.89/0.04/0.55 문턱과 covered/paraphrased/ad-lib 구간을 한 축에 표시
- [ ] S6 도표 — "실패의 단위" 막대가 ■■■■■■■■■ → ■■■ → ■ 로 줄어드는 시각화
- [ ] S8 도표 — `화면 전환 → 이미지 준비`와 `이미지 준비 → 화면 전환`의 순서 차이를 가장 크게 표시
- [ ] S8 운영 측정 — 같은 서버 프로젝트에서 `1 → 2 → 1 → 2`를 5회 왕복하고 첫 전환·재방문 시간과 전송량을 기록. 임시값 `2.2초 → 0.6초`, `1.3초 → 0.05초`, `1.7MB → 0`을 실제 중앙값으로 교체할 것
- [ ] S8 네트워크 기록 — cold 최초 진입과 warm 재방문 기록을 각각 보존하고, hidden preview canvas가 0개인지 확인
- [ ] 마이크 권한·전체화면 권한을 시연 브라우저 프로필에서 사전 허용
- [ ] 4:30 통시연 3회 촬영, STEP별 실측 시간 기록 (초과 시 STEP 5를 20초로 압축)
- [ ] 백업 영상 4:30 (`artifacts/user-scenario-videos` 재활용), 로컬 재생 대기
