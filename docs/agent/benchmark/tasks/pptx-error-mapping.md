# Python PPTX error mapping benchmark

`services/python-worker/app/ai/pptx_ooxml_generation.py`의 오류 매핑에서 기존
status와 message 계약을 유지한 채 mapping 책임을 작은 함수로 추출하라. 가장
가까운 pytest에 회귀 사례를 추가하고 targeted test를 실행하라.
