# ORBIT 기술적 챌린지 분석

크래프톤 정글 SW·AI랩 "나만의 무기" 최종 발표용. 코드베이스와 git 이력에서 도출한 3개 챌린지를 정리했다.

**수치 표기 원칙**: 별도 표시가 없는 수치는 리포지토리의 문서·테스트·커밋·코드에 기록된 실측치다. `†` 표시는 발표용으로 재구성한 추정치이므로 사용 전 실측으로 대체하거나 추정임을 밝힐 것.

---

## 챌린지 1. 청중 200명 동시 응답에서 유실·중복·역전 없는 결과 집계

> 근거: `apps/api/src/activities/` (`activity-response.repository.ts`, `activity-responses.service.ts`, `activity-load.integration.spec.ts`), `apps/api/src/presentation-sessions/` (`audience-access-cookie.ts`, `audience-rate-limit.service.ts`), `apps/api/src/database/migrations/2026071702000-CreateActivityRuntime.ts`, `apps/web/src/features/activity-slides/model/` (`activityRevision.ts`, `activityRealtimeClient.ts`), `docs/qa/activity-slides/h4-200-audience-load.md` (2026-07-17), 커밋 `724299d43`, `df4a724aa`

### 발생한 문제 (Issue)

참여 장표(만족도 조사·투표)는 발표 중 청중이 모바일로 동시에 응답을 제출하고, 발표자 화면에 집계가 실시간 반영되는 기능이다. 최종 시연 목표는 **청중 200명 동시 응답**이었고, 출시 게이트(H4)로 "실패·유실·중복 0, 최종 count 200, HTTP p95 2초 이하"를 걸었다.

2026-07-17 H4 검증을 준비하며 초기 구현을 검토한 결과 네 가지 결함 경로가 확인됐다.

1. `activity_runs.response_count`(비정규화 카운터)와 `revision`을 읽고-증가시키는 로직이 동시 제출에서 **lost update**를 일으킬 수 있음 — 200명이 제출해도 count가 200 미만이 되는 문제
2. 발표자 화면이 Socket.IO 이벤트 payload를 그대로 렌더링하면, 이벤트 **순서 역전·유실·재연결** 시 낡은 집계가 최신 집계를 덮어씀
3. 청중이 새로고침하거나 모바일 네트워크에서 요청을 재전송하면 같은 사람이 **2회 카운트**됨
4. run이 마감(closed)되는 순간과 제출이 겹치면 **마감된 run에 응답이 들어감**

### 원인 추론 및 분석 (Troubleshooting)

**가설 1 — "UPDATE ... SET count = count + 1 원자 증가만으로 충분하다?"** 카운터 하나면 충분하지만, 실제 쓰기는 "응답 upsert + 주관식 텍스트 엔트리 sync + count/revision 증가"가 한 단위다. 게다가 run 상태 검증(open인지, 세션이 live인지)과 쓰기 사이에 상태가 바뀔 수 있다. 검증과 쓰기가 **하나의 직렬화 지점**을 공유해야 한다는 결론.

**가설 2 — "Socket.IO가 이벤트 순서를 보장해주지 않을까?"** 재연결 구간, 복수 이벤트의 브로드캐스트 타이밍을 고려하면 수신 순서는 보장 대상이 아니다. 이벤트를 데이터 전달 수단으로 쓰는 한 역전 문제는 클라이언트에서 해결할 수 없다.

**가설 3 — "중복은 프론트에서 버튼을 잠그면 되지 않을까?"** UI 잠금은 새로고침·재전송·악의적 요청을 못 막는다. 중복 차단은 서버의 식별자와 저장소 제약으로 내려가야 한다.

검증은 추측 대신 **재현 가능한 부하 harness**로 했다. 200개의 고유 signed audience cookie를 만들어 동시 제출하고, 제출 동안 `pg_locks`를 폴링해 run row의 lock 대기자 수를 샘플링하며(`sampleActivityRunLockWaiters`), Socket.IO revision 수신과 `perf_hooks` 기반 latency를 함께 추적하는 통합 테스트를 작성했다(`activity-load.integration.spec.ts`). 환경변수가 없으면 suite를 skip해 일반 CI를 오염시키지 않고, fixture는 `afterAll`에서 삭제한다.

### 해결 방법 (Solution)

방어선을 네 겹으로 설계했다. 각 겹이 서로 다른 실패 모드를 막는다.

**1겹 — 청중 식별: HMAC 서명 쿠키.** 청중은 로그인이 없으므로, 입장 시 `audienceId`를 발급하고 HMAC-SHA256 서명과 User-Agent 바인딩을 붙인 `httpOnly` 쿠키로 고정한다(`audience-access-cookie.ts`). 새로고침해도 같은 사람은 같은 `audienceId`다. 위조 쿠키는 서명 검증에서 거부되고, audience별 60초 window rate limit(`audience-rate-limit.service.ts`)이 폭주를 차단한다.

**2겹 — 쓰기 직렬화: 의도적으로 좁힌 단 하나의 lock.** 트랜잭션 안에서 대상 run row만 `FOR UPDATE`로 잠그고, 세션 유효성 검증까지 같은 쿼리에서 끝낸다.

```sql
SELECT runs.activity_run_id, ..., runs.revision
FROM activity_runs AS runs
INNER JOIN presentation_sessions AS sessions ON ...
WHERE ... AND runs.is_current AND runs.status = 'open'
  AND sessions.status = 'live' AND sessions.results_deleted_at IS NULL
  AND sessions.starts_at <= now() AND sessions.expires_at > now()
FOR UPDATE OF runs   -- run row만 잠금, sessions는 잠그지 않음
```

lock을 잡은 뒤에야 응답 upsert → 텍스트 엔트리 sync → `revision + 1`(신규 제출일 때만 `response_count + 1`)이 진행되므로, 마감 직전 경합(문제 4)과 lost update(문제 1)가 같은 지점에서 해소된다.

**3겹 — 멱등성: 애플리케이션과 DB 이중 차단.** 클라이언트가 제출마다 `clientMutationId`를 보내고, 서버는 같은 ID의 재전송이면 revision을 올리지 않고 조기 반환한다. 불필요한 브로드캐스트도 함께 억제된다.

```ts
// activity-responses.service.ts — 재전송이면 아무것도 바꾸지 않는다
if (existing?.last_client_mutation_id === input.clientMutationId) {
  return { changed: false, response: existing, runRevision: target.revision };
}
```

애플리케이션 로직이 뚫려도 DB 제약이 최후 방어선이다.

```sql
CONSTRAINT uq_activity_responses_run_audience UNIQUE (activity_run_id, audience_id)
-- + uq_activity_runs_one_open_per_session: 세션당 open run은 항상 1개
```

**4겹 — 읽기 경로: 이벤트는 신호, 데이터는 권위 스냅샷.** 이벤트는 트랜잭션 **commit 이후**에만 발행하고, payload에는 집계 데이터가 아니라 `(runId, revision)`만 담는다. 클라이언트는 현재보다 **높은 revision일 때만 HTTP snapshot을 재조회**하고, 같은 run의 낮거나 중복된 revision은 무시한다. 새 run은 revision이 낮게 시작해도 run ID가 다르므로 수용한다.

```ts
// activityRevision.ts — 단조 증가 가드
if (current?.runId === runId && current.revision >= revision) return false;
```

재연결은 별도 보상 경로를 둔다. `connect` 시 세션에 join한 뒤 **무조건 refetch**해, 끊겨 있던 동안 유실된 이벤트를 스냅샷 재조회로 흡수한다(`activityRealtimeClient.ts`). 결과 갱신·상태 전환·활성 장표 변경 3종 이벤트가 전부 같은 revision consumer 하나로 수렴하므로 가드 로직도 한 곳에만 있다.

버린 선택: 이벤트에 집계를 실어 재조회를 없애는 안은 역전 방어를 클라이언트 곳곳에 심어야 해서 기각. Postgres advisory lock은 세션 유효성 검증과 잠금을 한 쿼리로 묶을 수 없어 row lock을 택했다.

### 결과 및 회고 (Learnings)

H4 실측 결과 (2026-07-17, `h4-200-audience-load.md`):

| 항목 | 결과 | 기준 |
| --- | ---: | --- |
| HTTP 성공 | 200/200 | 실패 0 |
| p50 / p95 | 191ms / **325ms** | p95 2,000ms 이하 (예산의 16.3%) |
| 응답 row / 고유 audience | 200 / 200 | 유실·중복 0 |
| 최종 run revision (DB = Socket.IO) | 201 = 201 | 일치 |
| 관측된 최대 lock 대기자 | 9 | 직렬화 확인 |
| 최종 이벤트 수신 → 권위 snapshot 확보 | 6ms | — |

run row 직렬화로 최대 9개의 lock 대기가 생겼지만 p95는 예산의 16.3%에 그쳤다. **경합을 없애는 것이 아니라, 경합 지점을 하나로 좁히고 그 비용을 측정으로 확인하는 것**이 실용적 해법이었다.

배운 것: (1) 동시성 결함은 한 겹으로 못 막는다 — 식별(쿠키)·직렬화(row lock)·멱등성(mutation ID + UNIQUE)·읽기 정합(revision)이 각자 다른 실패 모드를 담당하게 층을 나눠야 한다. (2) 실시간 UI에서 push payload를 진실로 삼지 않는 패턴(이벤트=무효화 신호, 데이터=권위 스냅샷)은 역전·유실·재연결 문제를 클라이언트 로직 한 줄(단조 증가 가드)로 수렴시킨다. (3) 부하 검증을 재현 가능한 통합 테스트로 남기고 lock 대기자 수까지 측정하면 "동작한다"가 아니라 "왜 안전한지"를 설명할 수 있다.

---

## 챌린지 2. "대본을 토씨까지 읽어야 진행되는 프롬프터" — E5 임베딩 기반 오인식·의역 보정

> 근거: `apps/web/src/features/rehearsal/speech/` (`e5EmbeddingService.ts`, `semanticUtteranceDecision.ts`, `semanticUtteranceMatcher.ts`, `prompterLexicalEvidence.ts`, `pauseDetector.ts`), `apps/web/src/features/rehearsal/stt/` (`alternativeReranker.ts`, `rerankingLiveSttPort.ts`), `docs/spikes/e5-prefix-mode-calibration.md` (2026-07-09), `docs/spikes/semantic-cue-browser-nli-poc.md` (2026-07-10), 커밋 `c37cff126`, `4970445d0`, `be8ee2921`, `86d47b9eb`, `bc495a482`

### 발생한 문제 (Issue)

리허설 모드의 자동 프롬프터는 발화를 STT로 인식해 대본의 현재 문장을 따라가며 강조를 이동시킨다. 초기 구현은 어절 단위 **lexical(문자열) 일치**만 사용했는데, 2026-07 초 내부 리허설 QA에서 두 가지 실패가 반복됐다.

1. 대본과 조금만 다르게 말하면(의역, 조사·어미 변형) 프롬프터가 그 문장에서 **멈춰버림**. 한국어 STT 오인식까지 겹치면 대본을 정확히 읽어도 멈춤
2. 반대로 대본과 무관한 애드립이 우연히 어절 몇 개와 겹치면 프롬프터가 **잘못 진행됨** — 발표 중 오진행은 멈춤보다 치명적

즉 "리허설을 도와주는 도구"가 "대본을 토씨까지 따라 읽게 강제하는 도구"가 되어 있었다.

### 원인 추론 및 분석 (Troubleshooting)

**시도 1 — STT 자체를 개선하면 되지 않을까?** Web Speech phrases로 발표 키워드에 인식 가중치를 주고, final 대안 후보들을 대본 어휘로 재순위하는 모듈(`alternativeReranker.ts`, `rerankingLiveSttPort.ts`)을 만들어 키워드 인식률은 개선했다. 그러나 이는 오인식 축소일 뿐 **의역은 원리적으로 해결 불가**했다. 문제를 "인식 정확도"가 아니라 "발화-대본 매칭의 의미 이해"로 재정의했다.

**시도 2 (실패) — NLI(자연어 추론) 모델로 함의 판정.** `MoritzLaurer/multilingual-MiniLMv2-L6-mnli-xnli`를 Transformers.js로 브라우저에서 실행하는 PoC를 진행했다. warm 추론은 p95 44.8ms로 빨랐지만 배포 비용이 문제였다.

| 항목 | 실측 (2026-07-10) | 게이트 |
| --- | --- | --- |
| 모델 아티팩트 | fp32 ONNX **428MB** (양자화판 없음) | — |
| WebGPU cold load | 45초 내 로드 실패 (timeout) | 45s |
| WASM cold load | 44.3s / 재실행 시 45s 초과 | 45s |
| false covered 검증 | fixture 4개 → 1% 게이트 검증 불가 | 음성 예제 100+ 필요 |

→ 어떤 backend도 인증 실패, 기본 off로 봉인. "더 똑똑한 모델"이 아니라 **로딩 시간과 아티팩트 크기라는 배포 제약**이 기각 사유였다.

**시도 3 — 경량 임베딩 유사도.** `Xenova/multilingual-e5-small`(384차원)을 Transformers.js feature-extraction으로 실행. 단, E5는 모델 카드상 cosine 점수가 **0.7~1.0 구간에 몰리는** 특성이 있어 "절대 임계값 하나로 애드립을 거절"하는 것이 불가능했다. 그래서 구현 전에 스파이크로 (a) prefix 모드 — 짧은 문장 간 대칭 비교이므로 `query:`/`query:`가 `query:`/`passage:`보다 적합한지, (b) 임계값 조합을 한국어 발표 fixture(정확 낭독·STT 노이즈·의역·인접 문장 모호·애드립·혼합 발화)로 캘리브레이션했다. 결정 우선순위 1번은 명시적으로 **"애드립 false positive(오진행) 최소화"** — 오판 비용이 비대칭이기 때문이다.

### 해결 방법 (Solution)

**판정 사다리.** 캘리브레이션으로 고정한 정책(`adLibRejectThreshold: 0.89`, `ambiguousMargin: 0.04`, `exactLexicalThreshold: 0.55`)을 4단계 거절-우선 사다리로 적용한다. 수용은 마지막에만 일어난다.

```ts
// semanticUtteranceDecision.ts — 거절 우선 판정 사다리 (요약)
if (!first || first.similarity < 0.89)      return reject("ad-lib");            // 1. 애드립 거절
if (first.covered)                          return reject("rejected-covered");  // 2. 이미 커버된 문장 재수용 차단
if (first.sim - second.sim < 0.04)          return reject("rejected-ambiguous");// 3. 인접 문장과 모호하면 보류
const outcome = lexicalOverlap >= 0.55 ? "covered" : "paraphrased";             // 4. 수용: 정확 낭독 vs 의역
```

2단계(covered 재수용 차단)가 실전에서 중요했다. 이미 소화한 문장을 반복하거나 되짚어 말할 때 프롬프터가 뒤로 튀는 것을 막는다. 4단계의 lexical overlap은 단순 포함 비교가 아니라 **단어 multiset recall**(`calculateWordMultisetRecall`)로, 대본 문장의 단어를 얼마나 회수했는지를 잰다.

**검색 구조.** 유사도 비교는 문장 전수 비교가 아니다. 슬라이드별로 대본 문장을 `embedPassages`로 배치 임베딩해 인덱스를 만들고 **`speakerNotesHash`로 캐시**한다 — 대본이 안 바뀌면 재임베딩하지 않고, 바뀌면 해시가 달라져 자동 무효화된다. 발화는 `embedQuery` 1회 후 코사인 상위 `topK: 3`만 판정 사다리에 올린다. 모델은 리허설 준비 단계에서 lazy-load하고 모듈 싱글턴(`sharedServicePromise ??= ...`)으로 공유해 발표 시점 cold load를 0으로 만들었다.

**E5를 단독 근거로 쓰지 않는 하이브리드.** 순수 semantic 진행을 시험하자 인접 문장 조기 승계·이전 판정의 뒤늦은 적용 같은 새 오작동이 생겼다(커밋 `8a670386a`, `3038a40ee`). 최종 구조는 역할 분리다.

- **프롬프터 진행(현재 문장 이동)**: lexical evidence를 앵커로, E5는 최소한의 lexical evidence가 있는 final 결과에만 보조 근거로 사용 (`4970445d0`)
- **코칭 커버리지(대본 소화 판정)**: semantic 분류(covered/paraphrased badge)를 사용하되 진행과 독립된 상태로 유지 (`be8ee2921`, `86d47b9eb`)

이때 lexical evidence 누적기 자체도 오진행 방어적으로 설계했다. 단어별 인정 횟수를 대본 등장 횟수로 상한 걸어, 애드립에서 같은 단어를 반복해도 evidence가 부풀지 않는다.

```ts
// prompterLexicalEvidence.ts — 대본에 없는 단어는 증거가 아니고, 있는 단어도 등장 횟수까지만
const maximumUsefulCount = scriptWordCounts.get(word) ?? 0;
if (maximumUsefulCount === 0) continue;
const nextObservedCount = Math.min(maximumUsefulCount, Math.max(previousObservedCount, count));
```

문장 경계 확정은 침묵 기준 `pauseMs: 600`(음량 `-55dB` 이하)으로 정렬하고, 599ms 이하에서는 진행하지 않는 경계 테스트로 고정했다(`bc495a482`).

### 결과 및 회고 (Learnings)

의역·경미한 오인식에서 프롬프터가 멈추지 않고, 애드립 오진행은 0.89 + 마진 + covered 재수용 차단으로 방어된다(스파이크 fixture 세트 통과 기준). 대본 완료 표시와 의미 커버리지가 분리되어 "정확히 읽었는지"와 "의미를 전달했는지"를 각각 보여줄 수 있게 됐다. †리허설 1회당 프롬프터 수동 복구 횟수 감소치는 재구성 필요.

배운 것: (1) 모델 선정의 결정 변수는 정확도가 아니라 **배포 제약(428MB/45s cold load)**일 수 있다 — 실패한 NLI PoC를 문서로 남겨 재논의 비용을 없앴다. (2) 점수 분포가 왜곡된 임베딩 모델은 절대 임계값 대신 순위·마진·이종 신호(어휘 중첩) 결합, 그리고 거절-우선 판정 순서로 다뤄야 한다. (3) 오판 비용이 비대칭인 판정은 "무엇을 틀려도 되는가"를 먼저 정하고 캘리브레이션해야 하며, 판정 결과는 비용이 큰 경로(진행)와 작은 경로(badge)로 분리 적용하는 것이 안전하다. (4) 임베딩 도입 후에도 lexical 신호를 버리지 않았다 — 두 신호의 실패 모드가 다르므로 서로의 방어선이 된다.

---

## 챌린지 3. AI PPT 생성 파이프라인 — stage 분리와 Postgres 기반 전송 계층 구축

> 근거: `docs/plans/ai-ppt-341-339-338-integrated-execution-plan.md` (2026-07-14), `ai-ppt-slide-streaming-generation.md`, `apps/worker/src/generate-deck/` (`postgres-stage-runner.ts`, `stage-checkpoint-repository.ts`, `stage-reconciler.ts`, `transport-failure-recovery.ts`), `packages/shared/src/config/runtime.ts` (실행 모드), `.env.example`, 커밋 `8911390c4`, `f732449aa`, `6b1ee637e`(PR #415), `2d9ba635f`, `544f5768d`, `b1567a30f`

### 발생한 문제 (Issue)

초기 AI PPT 생성은 **단일 Job이 Python worker의 `/ai/generate-deck` 한 호출**로 전 단계(brief 해석 → 자료 grounding → 내러티브 → 디자인 → 레이아웃 → 품질 검수 → 이미지·QA·발행)를 실행하는 monolith였다. 생성이 수 분 걸리는 동안 중간 산출물이 없고, 후반 단계 실패 시 **덱 전체를 처음부터 재실행**해 이미 성공한 앞 단계의 LLM 호출이 통째로 낭비됐다.

그래서 파이프라인을 stage 단위로 분리하기로 했는데, 분리하는 순간 새 문제 계층이 생긴다. stage를 여러 worker에 걸쳐 실행하면 (1) worker 사망으로 stage가 영원히 "진행 중"에 고착되는 문제, (2) 같은 stage를 두 worker가 집는 중복 실행, (3) 전송 계층과 DB 상태 저장소의 불일치를 모두 감당해야 한다. 즉 이 챌린지의 본질은 "쪼개기"가 아니라 **쪼갠 뒤의 분산 실행을 신뢰 가능하게 만들 전송 계층의 설계**였다.

### 원인 추론 및 분석 (Troubleshooting)

**1단계 — 계측으로 monolith의 한계 확인.** 단계별 시간 계측(`8911390c4`, 07-07)과 narrative 캐시·축소 프롬프트 등 각개 최적화를 먼저 했지만, 근본 원인은 속도가 아니라 **실패·재시도의 단위가 "전체 Job"이라는 구조**였다. stage 분리(#338: checkpoint, 부분 재시도, OCR·슬라이드 shard fan-out)는 그래서 확정됐고, 문제는 분리된 stage의 실행 신뢰성으로 좁혀졌다.

**2단계 — staged BullMQ 운영에서 만난 실제 버그.** 07-15, BullMQ(Redis) 기반 staged 실행 중 **마지막 재시도 attempt가 실패하면서 DB에 반영되지 않으면 Job이 영원히 "running"으로 고착**되는 문제가 발생했다. 최종 attempt의 catch에서 awaited DB recovery를 연결해 고쳤지만(`f732449aa`), 이는 근본 해결이 아닌 보완 조치다.

```ts
} catch (error) {
  await this.recoverAiDeckTransportFailure(queueName, job); // 큐(Redis)와 상태(PG)가 분리라서
  this.logger.error({ event: "job.failed", ... });          // 필요해진 '정합 복구' 계층
```

근본 원인은 **큐(Redis)와 상태 저장소(Postgres)가 서로 다른 시스템**이라는 것 — 이중 쓰기의 정합성 문제다. 그리고 이 문제는 외부 큐를 쓰는 한 transport를 무엇으로 바꿔도 남는다. 메시지 확인 처리와 DB checkpoint commit은 여전히 두 시스템이기 때문이다.

**3단계 — 요구사항의 재정의.** 전송 계층에 실제로 필요한 것을 목록으로 환원했다 — 전달 보장, 경합 없는 수신, 처리 중 재전달 방지(lease), 재시도 상한과 실패 격리, 중복 방어. 전부 **특정 제품이 아니라 큐의 의미론**이었고, 이미 운영 중인 Postgres의 row lock과 lease로 구현 가능했다. 별도 큐 시스템을 유지하면 인프라가 팀원별 staging 환경 수만큼 복제되고, 로컬·CI에 mock 계층이 필요하며, 2단계에서 확인한 이중 쓰기 문제가 그대로 남는다.

**4단계 — 결정.** 2026-07-16 PostgreSQL transport를 구현하고(`6b1ee637e`, PR #415, 동시성 계약 테스트 `2d9ba635f`), 07-17부터 기본 실행 경로로 전환했다.

### 해결 방법 (Solution)

**큐 의미론의 Postgres 구현.** `AiDeckPostgresStageRunner`가 checkpoint 테이블 자체를 큐로 사용해 폴링·선점·실행한다. 별도 메시지 브로커를 거치지 않는다.

| 전송 계층 요구사항 | Postgres 구현 (`stage-checkpoint-repository.ts`) |
| --- | --- |
| 경합 없는 메시지 수신 | `FOR UPDATE OF stages SKIP LOCKED`로 stage row claim |
| 처리 중 재전달 방지 | `lease_expires_at = now() + interval '10 minutes'` + 진행 중 연장, reconciler가 만료 lease 스캔·재큐잉 |
| 재시도 상한·실패 격리 | attempt 소진 시 terminal failure 기록, `failedStage` 보존 → 명시적 부분 재시도 API |
| 중복·순서 방어 | 결정적 stage identity + `shardKey` fencing (claim과 메시지 불일치 시 거부) |
| 메시지 수신 대기 | `pollIntervalMs` 주기 폴링 |

```sql
-- 큐 = 테이블: 대기 중 stage를 경합 없이 선점 (stage-checkpoint-repository.ts)
SELECT ... FROM ai_deck_generation_stages AS stages
WHERE stages.status = 'queued' AND stages.attempt < 5   -- 재시도 상한
ORDER BY stages.created_at, ... LIMIT 1
FOR UPDATE OF stages SKIP LOCKED;
UPDATE ... SET status = 'running', attempt = attempt + 1,
  lease_owner = $2, lease_expires_at = now() + interval '10 minutes'
```

**단일화가 없애준 것: 이중 쓰기 복구 계층.** 큐와 상태 저장소가 같은 DB가 되면서, stage 선점·artifact 저장·상태 전이가 **한 트랜잭션**으로 묶인다. 최종 stage(publication)는 execution artifact + checkpoint + Deck upsert + 부모 Job 성공을 한 번의 commit으로 끝낸다. `f732449aa`가 대표하던 "전송 계층-DB 정합 복구" 버그 클래스가 구조적으로 사라졌다. 동시성 계약은 별도 테스트로 고정했다(`2d9ba635f`: 이중 claim 불가, lease 만료 재선점).

**트레이드오프의 정직한 처리.** 폴링은 push보다 지연이 있고 DB에 부하를 준다. 다만 stage 하나가 초~분 단위 LLM 호출인 파이프라인에서 폴링 주기의 지연은 무시 가능하고, 폴링 부하 상한은 worker 수 × 주기로 예측 가능하다. 대규모 fan-out·다지역 확장으로 DB가 병목이 되는 규모에 도달하면 전용 메시지 브로커 도입을 재검토한다는 조건도 문서에 남겼다.

**실행 모드의 공존.** `AI_DECK_EXECUTION_MODE`는 `pg`(기본 실행 경로, 07-17부터 `.env.example`과 personal staging 기본값), `monolith`(회귀 검증·rollback 경로), `bullmq`(전환기 호환 경로)를 유지한다. 새 transport로의 전환도 되돌릴 수 있는 상태로 진행했다.

stage 분리 자체의 결과물(9개 stage 계약, checkpoint 부분 재시도, 슬라이드 shard, attempt당 LLM 1회의 스토리 경량화와 Story Review 승인 지점, 완료 슬라이드 연속 prefix를 즉시 보여주는 progressive preview)은 transport와 독립적으로 유지된다 — 전송 계층을 갈아끼워도 stage 계약이 그대로인 것 자체가 분리 설계의 증명이다.

### 결과 및 회고 (Learnings)

전송 계층을 위한 신규 인프라 없이 이미 compose로 운영하던 Postgres를 재사용한다. 별도 메시지 브로커의 구축·운영과 팀원별 staging 환경으로의 인프라 복제가 필요 없고, 로컬·CI·personal staging 어디서든 docker compose의 Postgres만으로 프로덕션과 동일한 큐 동작이 재현되므로 별도 mock 계층도 불필요하다. 정합성 측면에서는 이중 쓰기 복구 계층이 제거됐고, 실패 시 재실행 범위는 "덱 전체"에서 "실패 stage 또는 shard 1개"로 줄었다.

배운 것: (1) **필요한 것은 제품이 아니라 의미론이다** — 전달 보장·처리 중 재전달 방지·재시도 상한을 요구사항 목록으로 환원하니, 이미 가진 DB의 `SKIP LOCKED`와 lease로 충분했다. (2) **큐와 상태 저장소를 단일화하면 정합성 버그가 클래스째 사라진다** — BullMQ 경로의 실제 버그(`f732449aa`)가 이 결정의 가장 강한 근거였다. (3) 도입하지 않기로 한 결정도 재검토 조건과 함께 기록해야 한다 — "DB 폴링이 병목이 되는 규모"라는 조건을 남겨 같은 논의의 반복을 막았다. (4) transport 교체를 실행 모드 전환과 rollback 경로 위에서 진행해, 5주 일정에서도 되돌릴 수 있는 전환으로 만들었다.

---

## 부록: 재구성(†) 수치 확정을 위해 채울 것

- 챌린지 2: 도입 전후 리허설 1회당 프롬프터 수동 복구 횟수 (리허설 로그 `rehearsalLogCollector`로 산출 가능)
- 챌린지 3: monolith 대비 첫 슬라이드 노출 시점, shard 1개 실패 시 재실행 시간·LLM 호출 수 비교 (stage 계측 로그로 산출 가능)
