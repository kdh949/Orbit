# ORBIT 편집기 기능 격차 분석 — Google Slides / PowerPoint 대비

작성일: 2026-07-18
분석 방법: 코드베이스 직접 확인(grep·파일 검사) + 기존 감사 문서 대조 (`docs/feature-support-matrix.md`, `artifacts/product-audit-2026-07-15/ORBIT-24h-product-audit.md`)

## 결론 요약

**가장 빠르게 구현 가능하면서 사용자 만족도가 가장 높은 기능은 PDF 내보내기다.**

근거: (1) 내보내기 잡 파이프라인(`POST /deck/exports` → worker → Python)이 이미 완성되어 있고, (2) Python worker에 LibreOffice → PDF 변환 헬퍼가 **이미 구현되어 있어** (`services/python-worker/app/extraction.py:475~509`, 문서 파싱용으로 사용 중) 재사용만 하면 되며, (3) PDF는 공유·인쇄·제출 등 발표자료의 최종 전달 단계에서 거의 모든 사용자가 기대하는 형식이다. 예상 작업량 0.5~1일.

## 1. 문서 대비 코드 현행화 (중요)

기존 감사 문서(07-15)와 지원 매트릭스(07-17)가 지적한 P0/P1 갭 중 상당수가 **이미 해소됐다.** 이번 분석에서 코드로 재확인한 결과:

| 문서상 갭 | 현재 코드 상태 |
| --- | --- |
| 슬라이드 복제·삭제·재정렬 없음 | ✅ 구현됨 — `SlideNavigatorPane.tsx`에 `onDuplicateSlide`/`onDeleteSlide`/`onReorderSlides` |
| 캔버스 zoom 없음 | ✅ 구현됨 — `EditorZoomControl.tsx` + 테스트 |
| nudge·단축키 없음 | ✅ 구현됨 — `useEditorKeyboardShortcuts.ts`의 `nudge-selection` |
| viewer 역할 미반영 | ✅ 대체로 해소 — `EditorShell.tsx`의 `canMutateDeck` 게이팅 (capabilities 기반) |
| AI 경고 클릭-포커스 없음 | ✅ 구현됨 — `ValidationPanel.tsx`의 `onFocusTarget` |
| 스냅·정렬 가이드 없음 | ✅ 구현됨 — `canvasSnapping.ts`, `MultiSelectionQuickBar`의 정렬/배분 |
| deck 로드 실패 시 demo fallback | ⚠️ **여전히 존재** — `EditorShell.tsx:422` `deckQuery.data ?? fallbackDeck` |

즉 "편집기 기본기" 격차는 대부분 닫혔고, 남은 격차는 **문서 수명주기(내보내기·인쇄·공유)와 협업** 영역에 집중되어 있다.

## 2. Google Slides / PowerPoint 대비 현재 격차

코드 기준으로 확인된 미지원 기능 (2026-07-18):

| 격차 | 근거 | Slides/PPT에서의 위상 |
| --- | --- | --- |
| **PDF 내보내기** | `deckExportFormatSchema = ["pptx", "png"]` (`packages/shared/src/deck/deck-export.schema.ts:5`) | 핵심 — 공유·인쇄·제출의 기본 형식 |
| **찾기·바꾸기** | 텍스트 검색 코드 없음 (이미지 교체만 존재) | 기본 편집 기능 |
| **이미지 자르기(crop)** | 캔버스에 crop 구현 없음 | 기본 편집 기능 |
| **댓글·리뷰 협업** | 계약·코드 없음 | 협업 핵심 |
| **실시간 공동 편집** | 서버 게이트웨이만 존재, 웹에서 `canvas:update` 사용처 0건, Yjs import 0건 | Slides의 대표 기능 |
| **공개 링크 공유** | 계약 없음 (초대는 등록 사용자 중심) | 공유 기본 |
| **버전 시각 diff** | 스냅샷 목록·복원만 존재 | 보조 기능 |
| **슬라이드 전환 효과 편집 UI** | 스키마·발표 렌더러에 전환 개념은 있으나 편집 UI 미확인 | 기본 발표 기능 |
| **인쇄·페이지 설정** | 코드 없음 | PDF로 대부분 대체 가능 |
| **오프라인 편집** | 코드 없음 | 보조 기능 |

## 3. 우선순위 평가 — 구현 속도 × 사용자 만족도

| 후보 | 예상 작업량 | 만족도 영향 | 판단 |
| --- | --- | --- | --- |
| **PDF 내보내기** | **0.5~1일** (파이프라인·변환기 모두 기존 재사용) | **높음** — 전달 단계 필수, 감사에서 "미지원 형식이 메뉴에 노출"로 지적된 신뢰 문제도 함께 해결 | ⭐ **최우선 추천** |
| demo fallback 제거 → 오류/재시도 UI | 0.5일 | 중간 — 신뢰 회복이지만 정상 경로에선 안 보임 | 함께 처리 권장 |
| 찾기·바꾸기 | 1~2일 (deck 순회 + 기존 패치 연산 재사용) | 중간~높음 — 긴 덱 편집 시 체감 큼 | 2순위 |
| 이미지 crop | 2~3일 (Konva 마스킹 + PPTX 라운드트립 고려) | 중간 | 3순위 |
| 슬라이드 전환 UI | 1~2일 (스키마 기존, UI만) | 중간 | 3순위 |
| 공개 링크 공유 | 2~3일 + 보안 설계 | 높음 | 설계 선행 필요 |
| 댓글·리뷰 | 1주+ (신규 계약·엔터티·실시간) | 높음 | 중기 |
| 실시간 공동 편집(CRDT) | 수주 | 높음 | 장기 — `기술적-챌린지-분석.md` 참조 |

## 4. 추천 기능: PDF 내보내기 — 구현 스케치

기존 자산을 조립하는 수준이라 신규 설계가 거의 없다.

1. **계약**: `deckExportFormatSchema`에 `"pdf"` 추가 (`packages/shared/src/deck/deck-export.schema.ts`)
2. **Python worker**: 기존 PPTX 내보내기(`/ai/export-deck-pptx`) 결과물을 `extraction.py`의 LibreOffice 변환 헬퍼(`soffice --convert-to pdf`)로 후처리하는 엔드포인트 추가 — 변환 실패 처리 로직도 이미 존재
3. **Worker**: `deck-export.processor.ts`에 pdf 분기 추가 (PPTX 생성 → PDF 변환 → MinIO 업로드, 기존 흐름과 동일)
4. **웹**: `DeckExportDialog.tsx`에 PDF 라디오 옵션 추가 (PPTX/PNG와 동일 패턴)
5. **인프라 확인**: python-worker 컨테이너에 LibreOffice 설치 여부 확인 — 문서 파싱에서 이미 사용 중이므로 설치되어 있을 가능성 높음. 한글 폰트 임베딩 품질만 별도 확인 필요

리스크: LibreOffice 변환 품질(폰트·레이아웃)이 PPTX 원본과 다를 수 있음 → 기존 `tools/pptx-accuracy` 도구로 변환 품질 스팟 체크 권장.

## 5. 전략적 관점

감사 문서의 결론과 동일한 방향이다: ORBIT의 승부처는 Slides 기능 따라잡기가 아니라 **브리프 → AI 검사 → 리허설 → 발표** 여정이다. 편집기 기본기가 대부분 닫힌 지금, 남은 격차 중 "전달(내보내기)"만 닫으면 *만들고 → 다듬고 → 연습하고 → 전달한다*의 전체 루프가 완성된다. 협업(댓글·공동 편집)은 만족도는 높지만 작업량이 커서, PDF·찾기바꾸기 같은 단기 승수를 먼저 처리한 뒤 별도 설계로 진행하는 것이 맞다.

## 부록: 분석 한계

정적 분석 기준이며 실제 실행 검증은 하지 않았다. `docs/feature-support-matrix.md`(07-17)는 슬라이드 조작·zoom·viewer 게이팅 항목이 현재 코드와 불일치하므로 갱신이 필요하다.
